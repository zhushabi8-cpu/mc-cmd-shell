package com.ppp.linux;/*
package com.ppp.Linux;

import com.ppp.Common.Unsupported;
import com.ppp.util.OSDetector;

public class LinuxShellExecutor {
    static {
        if (OSDetector.isWindows()) {
            executor = new LinuxShellExecutor();
        } else {
            executor = new Unsupported();
        }
    }
}
 */
